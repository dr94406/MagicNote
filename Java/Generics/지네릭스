지네릭스  -> JDK 1.5 출시

1.1 지네릭스란 ? 
다양한 타입의 객체를 다루는 메서드 또는 컬렉션의 컴파일 시 타입 체크를 해주는 기능.
객체 타입을 컴파일 시 체크하므로 객체 타입 안정성을 높이고 형변환의 번거로움을 줄임.

타입안정성을 높인다는 것은 ? 
의도하지 않은 타입의 객체가 저장되는 것을 방지하며 저장된 객체를 삿용할 때 기존 타입 -> 다른타입으로
잘못 변환되는 오류를 줄이고 방지해준다.

Ex)ArrayList를 꺼낼 때 마다 타입체크를 하고 형변환 하는 것은 불편하다.

지네릭스의 장점
1. 타입 안정성 제공
2. 타입체크와 형변환 생략 가능하므로 코드의 간결성 추가.
